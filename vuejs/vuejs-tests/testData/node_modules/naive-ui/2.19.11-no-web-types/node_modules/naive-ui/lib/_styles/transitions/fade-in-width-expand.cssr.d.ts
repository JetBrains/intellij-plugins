import { CNode } from 'css-render';
interface FadeInWidthExpandTransition {
    duration?: string;
    delay?: string;
}
export default function ({ duration, delay }?: FadeInWidthExpandTransition): CNode[];
export {};
