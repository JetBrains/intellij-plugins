\documentclass[11pt]{article}

\usepackage{amsmath}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% Spellcheck is not workng in <warning descr="COMMA_BEFORE_AND">comnts but</warning> <warning descr="EN_A_VS_AN">an</warning> grammar does

\begin{frame} % FIXME \begin{document} crash test environment
    \begin{itemize}
        \item $It are not checked in <warning descr="MORFOLOGIK_RULE_EN_US">formuls</warning>$
        \item It is <warning descr="ARTICLE_MISSING">friend</warning> of human.
        \item It is <warning descr="MORFOLOGIK_RULE_EN_US">frend</warning> of human
        \item It <warning descr="IT_VBZ">are</warning> working for <warning descr="MUCH_COUNTABLE">much</warning> warnings
    \end{itemize}

    Our goal was to minimize the code length, it <warning descr="IT_VBZ">are</warning> natural to compare the result with other methods.
    We have used arithmetic coding (AC) as an entropy encoder.
    <warning descr="UNLIKELY_OPENING_PUNCTUATION">,</warning> bad start of the sentence.

    \begin{itemize}
        \item are not bad
        \item two
        \item three
    \end{itemize}

    Our goal was to minimize the code length, it <warning descr="IT_VBZ">are</warning> natural to compare the result with other methods.
    We have used arithmetic coding (AC) an entropy super smart encoder for this.

    \begin{tikzpicture}
        \begin{axis}[
        mbarplot,
        symbolic x coords={DOI,FASTQ},
        xtick=data,
        xtick align=inside,
        enlarge x limits={abs=2.5cm},
        xlabel={It <warning descr="IT_VBZ">are</warning> error},
        ylabel={Compration rate, \%},
        width=\textwidth,
        ymin=0,
        height=6cm,
        ]
            \addplot table[x=Collection,y=gzip]{\mycompressiondata};
            \addplot table[x=Collection,y=bzip2]{\mycompressiondata};

            \legend{gzip, bzip2, v-grams+AC}

        \end{axis}
    \end{tikzpicture}


    В коробке лежало <warning descr="Sklonenije_NUM_NN">пять карандаша</warning>.
    А <warning descr="grammar_vse_li_noun">все ли ошибка</warning> найдены?
    Это случилось <warning descr="INVALID_DATE">31 ноября</warning> 2014 г.
    За весь вечер она <warning descr="ne_proronila_ni">не проронила и слово</warning>.
    Собрание состоится в <warning descr="RU_COMPOUNDS">конференц зале</warning>.
    <warning descr="WORD_REPEAT_RULE">Он он</warning> ошибка.

\end{frame}
