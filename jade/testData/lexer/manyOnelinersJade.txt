JS_META_CODE ('- function a(parameters) {\n-   var x = parameters.x;\n-   return x;\n- }\n\n')
EOL ('\n')
XML_TAG_NAME ('div')
INDENT ('\n    ')
JS_META_CODE ('- if (true)\n        div\n    - else\n        span\n    - if (1 + 1 == 2)\n        div\n    - else\n        span')
INDENT ('\n    ')
XML_TAG_NAME ('div')
EOL ('\n\n')
JS_META_CODE ('- var func = function(qwe) {\n-       return qwe * qwe;\n- };\n\n- var anotherFunc = function(param) {\n-   return param.param;\n- }')
EOL ('\n')
