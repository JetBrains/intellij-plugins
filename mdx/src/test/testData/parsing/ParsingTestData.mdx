import {
    ComponentHeader,
    ComponentFooter,
    DeprecatedComponentAlert,
} from 'components/thumbprint-components'
import {MdxDocument} from './mdxExample.mdx'

export default ({ children, ...props }) => <div {...props}>{children}</div>

<Button>  btn </Button>

<a
        href="https://github.com/thumbtack/thumbprint/issues/25"
        title="Details about the recent changes"
    >
        are out-of-date
</a>
