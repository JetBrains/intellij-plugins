/**
 * Provide service for old & new integration (ts-complier-host-impl)
 */
export declare function getService(serverFolderPath: string, acceptServices?: boolean): {
    context: any;
    serverFilePath: string;
};
