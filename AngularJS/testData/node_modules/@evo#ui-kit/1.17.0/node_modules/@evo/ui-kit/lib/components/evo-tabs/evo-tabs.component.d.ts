import { QueryList, AfterContentChecked, OnChanges, SimpleChanges } from '@angular/core';
export declare class EvoTabsComponent implements AfterContentChecked, OnChanges {
    isInited: boolean;
    tabsHeaderList: {
        label: string;
    }[];
    tabComponentsList: QueryList<any>;
    selectedIndex: number;
    ngAfterContentChecked(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onClick(index: number): void;
    changeSelectedTab(index: number): void;
}
