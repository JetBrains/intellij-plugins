import { AfterContentChecked, QueryList, SimpleChanges, OnChanges, EventEmitter } from '@angular/core';
export declare class EvoStepperComponent implements AfterContentChecked, OnChanges {
    isInited: boolean;
    stepsList: {
        label: string;
    }[];
    stepComponentsList: QueryList<any>;
    currentStepIndex: number;
    onChange: EventEmitter<number>;
    ngAfterContentChecked(): void;
    ngOnChanges(changes: SimpleChanges): void;
    changeCurrentStep(index: number): void;
}
