import { AbstractControl } from '@angular/forms';
export declare class FormHelper {
    static validateControls(control: AbstractControl): void;
    static resetControls(control: AbstractControl): void;
}
