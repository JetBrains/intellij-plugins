import { EventEmitter, OnInit } from '@angular/core';
export declare class MyLibComponent implements OnInit {
    constructor();
    ngOnInit(): void;
    predicate(arg: string): boolean;
    withSetter: boolean;
    readonly withGetter: EventEmitter<string>;
}
