import { EventEmitter, OnDestroy, OnInit } from '@angular/core';
import { Subscription } from 'rxjs';
import { EvoSidebarService } from './evo-sidebar.service';
export declare enum EvoSidebarCloseTargets {
    background = "background",
    button = "button"
}
export declare class EvoSidebarComponent implements OnDestroy, OnInit {
    sidebarService: EvoSidebarService;
    id: string;
    title: string;
    header: string;
    relativeFooter: boolean;
    onClose: EventEmitter<any>;
    keyUpSubscription: Subscription;
    states: {
        isVisible: boolean;
    };
    readonly closeTargets: typeof EvoSidebarCloseTargets;
    constructor(sidebarService: EvoSidebarService);
    ngOnDestroy(): void;
    ngOnInit(): void;
    closeSidebar(source: string): void;
    private subscribeToKeyEvent;
}
