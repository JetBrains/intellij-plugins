import { BehaviorSubject, Observable } from 'rxjs';
export declare enum EvoSidebarTypes {
    basket = "basket",
    partnerAuth = "partnerAuth",
    promoCode = "promoCode"
}
export interface EvoSidebarState {
    isOpen: boolean;
    params?: any;
}
export declare class EvoSidebarService {
    isSidebarVisible$: BehaviorSubject<{}>;
    sidebarEvents$: BehaviorSubject<any>;
    private registeredSidebars;
    private newRegisteredSidebars;
    deregister(id: string): void;
    register(id: string): void;
    open(id: string, params?: any): void;
    close(id: string, params?: any): void;
    getEventsSubscription(id: string): Observable<any>;
    getNewEventsSubscription(id: string): Observable<EvoSidebarState>;
}
