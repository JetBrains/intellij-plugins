import { OnDestroy, OnInit } from '@angular/core';
import { EvoModalService, EvoModalState } from './evo-modal.service';
export declare enum EvoModalTypes {
    warning = "warning"
}
export declare class EvoModalComponent implements OnInit, OnDestroy {
    private modalService;
    id: string;
    acceptText: string;
    declineText: string;
    type: EvoModalTypes;
    modalState: EvoModalState;
    private _id;
    constructor(modalService: EvoModalService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    handleOnClose(agreement: boolean): void;
    private subscribeModalEvents;
}
