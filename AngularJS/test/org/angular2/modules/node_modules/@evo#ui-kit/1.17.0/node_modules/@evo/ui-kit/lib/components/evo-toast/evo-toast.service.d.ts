import { EventEmitter } from '@angular/core';
import { EvoToastTypes } from './evo-toast.component';
export interface EvoToast {
    type?: EvoToastTypes;
    message: string;
}
export declare class EvoToastService {
    pushEvents: EventEmitter<EvoToast>;
    private isComponentRegistered;
    private isToastInProgress;
    private queue;
    constructor();
    push(toast: EvoToast): void;
    toastComplete(): void;
    register(): void;
}
