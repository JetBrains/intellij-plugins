import { AfterViewInit, OnChanges, SimpleChanges } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { FlatpickrOptions } from './flatpickr-options.interface';
export declare class EvoDatepickerComponent implements AfterViewInit, ControlValueAccessor, OnChanges {
    state: {
        isOpen: boolean;
    };
    private flatpickr;
    private defaultFlatpickrOptions;
    flatpickrElement: any;
    config: FlatpickrOptions;
    placeholder: string;
    setDate: string | Date;
    writeValue(value: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(): void;
    propagateChange: (_: any) => void;
    setDateFromInput(date: any): void;
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    /**
    * Customization of the flatpickr's view to ui-kit state
    */
    private customizePicker;
    /**
    * Sets new state of the picker openness
    * @param state new state
    */
    private setOpenedState;
    /**
    * Substitution of icons
    */
    private changeNextIcon;
}
