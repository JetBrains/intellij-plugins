import { OnInit, AfterContentInit } from '@angular/core';
import { ControlValueAccessor, FormBuilder, FormGroup } from '@angular/forms';
import { EvoBaseControl } from '../../../../common/evo-base-control';
import { IConstraints as IDataPickerConstraints } from '../evo-date-picker/evo-date-picker.component';
export interface IRangeConstraints {
    since: IDataPickerConstraints;
    until: IDataPickerConstraints;
}
export declare class EvoDatePickerRangeComponent extends EvoBaseControl implements ControlValueAccessor, OnInit, AfterContentInit {
    private formBuilder;
    _value?: Date[];
    style: 'input' | 'inline';
    formGroup: FormGroup;
    calendarSettings: {
        bigBanner: boolean;
        timePicker: boolean;
        format: string;
    };
    constraints: IRangeConstraints;
    rangeContols: {
        SINCE: string;
        UNTIL: string;
    };
    private disabled;
    onChange: (_: any) => void;
    onTouched: () => void;
    constructor(formBuilder: FormBuilder);
    value: Date[];
    ngOnInit(): void;
    ngAfterContentInit(): void;
    writeValue(value: Date[]): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    setDisabledState(state: boolean): void;
    onSelectRange(date: Date, selectedPicker: string): void;
    private createFormGroup;
    private setConstaints;
    private setConstaintsSince;
    private setConstaintsUntil;
}
