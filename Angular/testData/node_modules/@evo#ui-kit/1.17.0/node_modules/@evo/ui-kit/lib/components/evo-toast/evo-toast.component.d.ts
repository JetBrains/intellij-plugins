import { OnInit } from '@angular/core';
import { EvoToast, EvoToastService } from './evo-toast.service';
export declare enum EvoToastTypes {
    DEFAULT = "default",
    DANGER = "danger",
    SUCCESS = "success"
}
export declare class EvoToastComponent implements OnInit {
    private toastService;
    toast: EvoToast;
    private appearTimeout;
    constructor(toastService: EvoToastService);
    ngOnInit(): void;
    handleAnimationDone(): void;
    close(): void;
    private subscribeToToastPushes;
}
