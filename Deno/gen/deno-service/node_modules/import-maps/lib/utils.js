"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const url_1 = require("url");
function tryURLParse(str, baseURL) {
    try {
        return new url_1.URL(str, baseURL);
    }
    catch (e) { // TODO remove useless binding when ESLint and Jest support that
        return null;
    }
}
exports.tryURLParse = tryURLParse;
function tryURLLikeSpecifierParse(specifier, baseURL) {
    if (specifier.startsWith("/") || specifier.startsWith("./") ||
        specifier.startsWith("../")) {
        return tryURLParse(specifier, baseURL);
    }
    return tryURLParse(specifier);
}
exports.tryURLLikeSpecifierParse = tryURLLikeSpecifierParse;
