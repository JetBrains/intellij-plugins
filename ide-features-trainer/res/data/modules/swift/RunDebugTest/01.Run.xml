<xml type="Scenario" name="Run" id="swift.rdt.run" subtype="aimless" projectTree="hide" lang="Swift">
    <CopyText><![CDATA[import UIKit

class Rename: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()

        let x = 0
        let y = 50

        let tableView = UITableView()

        let header = UILabel()
        header.text = "AppCode"
        header.sizeToFit()

        tableView.frame = CGRect(x: x, y: y, width: 320, height: 400)
        tableView.tableHeaderView = header
        self.view.addSubview(tableView)
    }
}]]></CopyText>
    <MoveCaret position="6:10"/>
    <Try triggers="Run">There are two different actions for running and debugging the project in <ide/> (because in general Debug is slower). Try running your application using the <action>Run</action></Try>
    <Try triggers="ChooseRunConfiguration">Great! To select another Run Configuration or simulator for the current run configuration and quickly run your application, press <action>ChooseRunConfiguration</action>, select needed Run Configuration or simulator and press <action>EditorEnter</action>.</Try>
    <Win/>
</xml>