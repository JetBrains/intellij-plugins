<xml type="Scenario" name="Quick Fixes and intentions" id="swift.codegeneration.quickfixes" subtype="aimless" projectTree="hide" lang="Swift">
    <CopyText><![CDATA[import Foundation

class QuickFixes: NSObject {
    func firstWarning() {
        var unused1 = 0
        var unused2 = 1
    }

    func secondWarning() {
        var unused3 = 1
        print(__LINE__)
    }

    func typoInspection() {
        var incorrctlySpelled = "incorrctlySpeled"
        print(incorrctlySpelled)
    }

    func errorHandlingInspections() {
        let contents = String(contentsOfFile: "someFile")
        print(contents)
    }

    func redundantAttributes() {
        class Super {
            @objc func foo() {
            }

            @objc func bar() {
            }
        }

        class Sub: Super {
            @objc override func foo() {
            }

            @objc override func bar() {
            }
        }
    }

    func addRemoveExplicitType() {
        let fileManager = FileManager.default
        print(fileManager)
        let string: String = ""
        print(string)
    }

}]]></CopyText>
    <Text><ide/> shows all the same <code>fix-its</code> for your Swift code, as Xcode does.</Text>
    <MoveCaret position="6:17"/>
    <Try triggers="ShowIntentionActions">Press <action>ShowIntentionActions</action> and select <strong>Apply Fix-it</strong> to replace unused variable with <code>_</code></Try>
    <Try triggers="CodeInspection.OnEditor">Moreover, AppCode integrates SourceKit as a separate inspection, that means you can run it on the whole file and fix all the problems at once if several fix-its are available. Press <action>CodeInspection.OnEditor</action>.</Try>
    <Text>As you see, we have three problems in our code: two warnings and one error. Select <strong>Swift → SourceKit inspections</strong>, press <action>ShowIntentionActions</action> and select <strong>Apply Fix-it</strong> (or click the button with the same name on the right) - all the problems should be corrected. </Text>
    <MoveCaret position="15:19"/>
    <Try triggers="ShowIntentionActions;EditorChooseLookupItem">Now press <action>EditorEscape</action> to return to the editor window and then press <action>ShowIntentionActions</action> on the incorrectly spelled variable name. Select <strong>Type: Rename to...</strong>, choose the correct variant and press <action>EditorEnter</action></Try>
    <MoveCaret position="15:41"/>
    <Try triggers="ShowIntentionActions;EditorChooseLookupItem;ShowIntentionActions;EditorChooseLookupItem;">Note, how only the needed part was corrected and all variable usages were automatically renamed. Now repeat the same actions twice to fix the <code>incorrctlySpeled</code> string.</Try>
    <Try trigger="GotoNextError">Press <action>GotoNextError</action> to go to the next error</Try>
    <Try triggers="ShowIntentionActions">Error handling intentions help adding <code>try/catch/throws</code> where needed. Press <action>ShowIntentionActions</action> and select one of the quick-fixes.</Try>
    <Try trigger="GotoNextError">Press <action>GotoNextError</action> to go to the next warning</Try>
    <Try triggers="ShowIntentionActions">You can remove redundant <code>@objc</code> attributes - press <action>ShowIntentionActions</action> and select <strong>Remove redundant attribute @objc</strong></Try>
    <Try triggers="ShowIntentionActions">If need to add or remove explicit type to some variable, simply press <action>ShowIntentionActions</action> and select <strong>Add/Remove explicit type</strong>. Try it now with the <code>fileManager</code> variable</Try>
    <Win/>
</xml>