<xml type="Scenario" name="Duplicate" id="swift.editorbasics.duplicate" subtype="aimless" projectTree="hide" lang="Swift">
    <MouseBlock>
        <CopyText><![CDATA[import Foundation

public func fib(n: Int) -> Int {
    var a = 1;
    var b = 1;

    var tmp = 0

    if n < 2 {
        return 1
    }

    for _ in 0...n - 2 {
        tmp = b;
        b = a;
        a = a + tmp;
    }

    return a;
}

for i in 0...100 {
    print(fib(n: i))
}]]></CopyText>
    </MouseBlock>
    <MoveCaret position="7:9"/>
    <Try action="EditorDuplicate" trigger="EditorDuplicate">Duplicate any line with <action>EditorDuplicate</action>.</Try>
    <SetSelection start-position="10:1" end-position="13:1" />
    <Try action="EditorDuplicate" trigger="EditorDuplicate" check="training.check.CheckSelectedLines">You can do the same thing with multiple lines, too. Simply select two or more lines and duplicate them with <action>EditorDuplicate</action>.</Try>
    <Win/>
</xml>