{
  "name": "CanAutodetectLocalPackage",
  "version": "1.0.0",
  "devDependencies": {
    "eslint": "$ESLINT_VERSION$"
  },
  "eslintConfig": {
    "rules": {
      "semi": ["error", "always"]
    }
  }
}
