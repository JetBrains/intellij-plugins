description: Binding which defines some enum values for testing

compatible: "custom,enum"

properties:
  prop-int:
    type: int
    enum: [10, 11]

  prop-int-hex:
    type: int
    enum: [0x10, 0x11]

  prop-string:
    type: string
    enum: ["value1", "value2"]
