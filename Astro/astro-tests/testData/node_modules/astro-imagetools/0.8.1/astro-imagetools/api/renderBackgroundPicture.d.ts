import type {
  BackgroundPictureConfigOptions,
  BackgroundPictureHTMLData,
} from "0.8.1/astro-imagetools";

export default function renderBackgroundPicture(
  config: BackgroundPictureConfigOptions
): Promise<BackgroundPictureHTMLData>;
