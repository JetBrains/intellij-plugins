m() {
  "this string is longer than eighty characters since we add quite a few words to it";
}

n() {
  """
this string is longer than eighty characters since we add quite a few words to it
this one is also is longer than eighty characters since we add quite a few words to it
""";
}
