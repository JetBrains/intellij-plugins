resolve definition conflict (duplicate) via modification time

Critical: resource manager, need for *Formatter (see Form.mxml HSlider dataTip)

defaults.css в lib project

editor tab view
- close button
- tab label color (vcs — added, unversioned, etc.)

mxml
- targets="{[p1,p2,p3]}" p2 может быть динамическим объектом — на стороне сервера поддержка реализована, на стороне клиента нет.
- [1] поддержка includeIn/excludeFrom для Move — сейчас проблема в том, что мы не создаем parentContext для Transition (так как оно идет как значение для свойства transitions)
  <transitions>
    <!-- A transition for changing from any state to any state. -->
    <Transition id="myTransition" fromState="*" toState="*">
      <!-- Define a Parallel effect as the top-level effect.-->
      <Parallel id="t1" targets="{[p1,p2,p3]}">
        <!-- Define a Move and Resize effect.-->
        <Move duration="400"/>
        <Resize duration="400"/>
      </Parallel>
    </Transition>
  </transitions>
- test PopUpAnchor and support exeucte "frmPUA.displayPopUp=true" in runtime
- Эффекты для state specific attributes
- state specific attributes указанные как теги (<mouseDownEffect.Register>)
  
app
- отключать отображение element tools на момент проигрывания эффектов
  
  