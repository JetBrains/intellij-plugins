var Ext;
Ext.extend = function(p, o) {};
Ext.Container = function() {};

/**
 * @class Ext.Panel
 * @extends Ext.Container
 * <p>Panel is a container that has specific functionality and structural components that make ...</p>
 */
Ext.Panel = Ext.extend(Ext.Container, {
});
new Ext.Pan<caret>el();
