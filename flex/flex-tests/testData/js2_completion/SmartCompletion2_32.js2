class SmartCompletionTest {
    function SmartCompletionTest(x:Function) {
        new SmartCompletionTest(getU<caret>);
    }

    private function getUrl(x:int, y:int):String {
        return "someurl";
    }
}