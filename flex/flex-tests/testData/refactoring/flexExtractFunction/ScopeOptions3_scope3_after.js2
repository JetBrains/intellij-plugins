package {
class ClassName {
    function xxx() {
        var x:int = 1;
        var y:int = 1;
        var __ret:Object = created(x, y);
        x = __ret.x;
        y = __ret.y;
        alert(x);
        alert(y);
    }
}
}

function created(x:int, y:int):Object {
    var a = 1 + x + y;
    x = 2;
    y = 3;
    return {x: x, y: y};
}