package {
import flexlib.baseClasses.AccordionBase;
import flexlib.containers.HAccordion;

public class LibraryScope {
    public function LibraryScope() {
        var a:AccordionBase = new HAccordion();
    }
}
}
